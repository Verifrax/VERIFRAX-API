[package]
name = "verifrax-rust-verifier"
version = "0.1.0"
edition = "2021"
description = "Minimal Rust verifier for the VERIFRAX protocol"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
